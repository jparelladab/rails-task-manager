<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <%= link_to task.title, task_path(task) %>
  <%= link_to "Delete task", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %> <br>
<% end %>
<br>
<%= link_to "Add a new Task", new_task_path %>
